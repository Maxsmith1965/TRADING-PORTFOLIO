[build]
  command = "npm install"
  publish = "public"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@netlify/blobs"]

[functions."agent"]
  timeout = 60

[functions."marketdata"]
  timeout = 30

[functions."stocks"]
  timeout = 30

[functions."stocksearch"]
  timeout = 15
